language: java
os: linux
dist: xenial
jdk:
  - openjdk14
addons:
  sonarcloud:
    organization: "alegauss-github"
    token: ${SONAR_TOKEN}
script:
  - ./gradlew check
  -  sonar-scanner
after_success:
  - bash <(curl -s https://codecov.io/bash)
