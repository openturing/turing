package com.viglet.turing.converse;

import java.io.IOException;
import java.security.Principal;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.ModelAndView;

@Controller
public class TurConverse {

	/*
	 * @RequestMapping("/converse/{agentId}") private ModelAndView
	 * converseHome(HttpServletRequest request, HttpServletResponse response, String
	 * agentId, final Principal principal) throws IOException {
	 * 
	 * ModelAndView mav = new ModelAndView("converse/index");
	 * mav.addObject("agentId", agentId);
	 * 
	 * return mav;
	 * 
	 * }
	 */
}
